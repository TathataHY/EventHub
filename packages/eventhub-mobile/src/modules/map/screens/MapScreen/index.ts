/**
 * Implementación del patrón Container/Presenter para la pantalla de mapa
 * El patrón separa la lógica de negocio (Container) de la representación visual (Presenter)
 * para mejorar la mantenibilidad y facilitar las pruebas
 */

export { MapContainer as MapScreen } from './MapContainer';
export { MapPresenter } from './MapPresenter'; 