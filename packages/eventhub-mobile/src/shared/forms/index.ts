/**
 * Componentes de formularios compartidos
 */

// Exportar componentes de formularios
// Ejemplo: export * from './LoginForm'; 

// Ejemplo de componente básico para formularios
export const FormField = ({ label, children }: { label: string, children: React.ReactNode }) => {
  // Implementación futura
  return children;
}; 