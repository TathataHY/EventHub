// Exportación de tipos compartidos
// Aquí se incluirán interfaces y tipos utilizados en toda la aplicación

// Ejemplo de tipo compartido
export interface BaseEntity {
  id: string;
  createdAt?: Date;
  updatedAt?: Date;
} 