/**
 * Bandera para usar datos simulados en vez de llamadas reales a la API
 * Esta opción es útil para desarrollo local sin backend
 */
export const MOCK_MODE = true;

/**
 * URL base de la API
 */
export const API_BASE_URL = 'https://api.eventech.com/v1';

/**
 * Timeout para peticiones en milisegundos
 */
export const API_TIMEOUT = 15000;

/**
 * Versión de la aplicación
 */
export const APP_VERSION = '1.0.0';

/**
 * Environment actual (development, staging, production)
 */
export const ENVIRONMENT = 'development'; 